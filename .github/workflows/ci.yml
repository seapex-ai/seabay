name: CI

on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install ruff
        run: pip install ruff
      - name: Lint public Python surfaces
        run: |
          DIRS="backend adapters examples"
          [ -d apps ] && DIRS="$DIRS apps"
          [ -d agents ] && DIRS="$DIRS agents"
          [ -d shell-cli ] && DIRS="$DIRS shell-cli"
          ruff check $DIRS --extend-exclude "**/node_modules"

  test-unit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        working-directory: backend
        run: pip install ".[dev]"
      - name: Run unit tests (no DB required)
        working-directory: backend
        run: pytest tests/ -v --ignore=tests/integration

  test-integration:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_DB: seabay_test
          POSTGRES_USER: seabay
          POSTGRES_PASSWORD: seabay
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        working-directory: backend
        run: pip install ".[dev]"
      - name: Run migrations
        working-directory: backend
        env:
          SEABAY_DATABASE_URL: postgresql+asyncpg://seabay:seabay@localhost:5432/seabay_test
          SEABAY_REDIS_URL: redis://localhost:6379/0
        run: alembic upgrade head
      - name: Run all tests
        working-directory: backend
        env:
          SEABAY_DATABASE_URL: postgresql+asyncpg://seabay:seabay@localhost:5432/seabay_test
          SEABAY_REDIS_URL: redis://localhost:6379/0
        run: pytest tests/ -v

  docker:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [lint, test-unit]
    steps:
      - uses: actions/checkout@v6
      - name: Build Docker image
        run: docker build -t seabay-api:${{ github.sha }} backend/
      - name: Verify entrypoint exists
        run: |
          docker run --rm --entrypoint="" seabay-api:${{ github.sha }} \
            test -x ./entrypoint.sh
      - name: Verify Python imports
        run: |
          docker run --rm --entrypoint="" seabay-api:${{ github.sha }} \
            python -c "from app.main import app; print('OK: app imports successfully')"

  # ── MCP Edge syntax check (skips if dir absent) ──
  mcp-edge:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v5
        if: hashFiles('apps/mcp-edge/main.py') != ''
        with:
          python-version: "3.12"
      - name: Syntax check MCP Edge
        if: hashFiles('apps/mcp-edge/main.py') != ''
        run: find apps/mcp-edge -name "*.py" -exec python -m py_compile {} +
      - name: Skip (dir not present)
        if: hashFiles('apps/mcp-edge/main.py') == ''
        run: echo "apps/mcp-edge/ not present, skipping"

  # ── Shell CLI syntax check (skips if dir absent) ──
  shell-cli:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v5
        if: hashFiles('shell-cli/seabay_shell/main.py') != ''
        with:
          python-version: "3.12"
      - name: Syntax check Shell CLI
        if: hashFiles('shell-cli/seabay_shell/main.py') != ''
        run: find shell-cli -name "*.py" -exec python -m py_compile {} +
      - name: Skip (dir not present)
        if: hashFiles('shell-cli/seabay_shell/main.py') == ''
        run: echo "shell-cli/ not present, skipping"

  # ── Seed Agents syntax check (skips if dir absent) ──
  seed-agents:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v5
        if: hashFiles('agents/translator/agent.py') != ''
        with:
          python-version: "3.12"
      - name: Syntax check all agents
        if: hashFiles('agents/translator/agent.py') != ''
        run: find agents -name "*.py" -exec python -m py_compile {} +
      - name: Skip (dir not present)
        if: hashFiles('agents/translator/agent.py') == ''
        run: echo "agents/ not present, skipping"
