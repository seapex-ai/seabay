"""MCP Edge P0 Tools — 6 tools for V1.0.

Each tool follows MCP tool schema format:
- Tool name, description, input schema (JSON Schema)
- Handler function that calls Core API
- Response in MCP format: summary_text, data, next_actions, fallback_url
"""
